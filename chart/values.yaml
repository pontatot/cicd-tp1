debug: false

replicaCount: 2

image:
  repository: harbor.pyxis.dopolytech.fr/main/city-api
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

postgresql:
  auth:
    username: postgres
    password: postgres
    database: city_api
